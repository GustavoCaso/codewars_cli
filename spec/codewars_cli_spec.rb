require 'spec_helper'

describe CodewarsCli do
  it 'has a version number' do
    expect(CodewarsCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
